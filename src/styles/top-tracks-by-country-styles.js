import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
    itemContainer: {
        borderBottomWidth: 0
    },
    rightIcon: {
        marginRight: 10,
        fontSize: 24,
        color: 'red'
    }
});

export default styles;
